# Ticketing Platform Environment Configuration
# Copy this file to .env and customize for your environment

# Application Environment
ASPNETCORE_ENVIRONMENT=Development

# Database Configuration (choose one method)

# Method 1: Direct PostgreSQL connection string (recommended for local development)
POSTGRES_CONNECTION=Host=localhost;Port=5432;Database=ticketing_platform_dev;Username=ticketing_admin;Password=ticketing_dev_password;Sslmode=Disable

# Method 2: Database URL format (Supabase/Heroku style - for production)
# DATABASE_URL=postgres://username:password@hostname:port/database

# Docker Compose database connection (use when running with docker-compose)
# POSTGRES_CONNECTION=Host=ticketing-db;Port=5432;Database=ticketing_platform_dev;Username=ticketing_admin;Password=ticketing_dev_password;Sslmode=Disable

# Optional: Schema prefix for multi-tenant or environment isolation
# DB_SCHEMA_PREFIX=dev

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-must-be-at-least-32-characters-long-for-security
JWT_ISSUER=TicketingPlatform
JWT_AUDIENCE=TicketingPlatform
JWT_EXPIRATION_MINUTES=1440

# Logging Configuration
ASPNETCORE_LOGGING__LOGLEVEL__DEFAULT=Information
ASPNETCORE_LOGGING__LOGLEVEL__MICROSOFT=Warning

# API Configuration
ASPNETCORE_URLS=http://localhost:5000;https://localhost:5001

# CORS Configuration
# Comma-separated list of allowed origins for Cross-Origin Resource Sharing
# Examples:
# Development (allow local frontend):
# CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,https://localhost:3000

# Production (specific domains only):
# CORS_ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com,https://your-frontend.vercel.app

# If not set, will use values from appsettings.json based on environment
# CORS_ALLOWED_ORIGINS=

# Production Database Configuration Examples:

# Supabase
# DATABASE_URL=postgres://postgres.xyz:password@aws-0-us-west-1.pooler.supabase.com:5432/postgres

# Azure PostgreSQL
# POSTGRES_CONNECTION=Host=myserver.postgres.database.azure.com;Port=5432;Database=ticketing_platform;Username=myadmin@myserver;Password=mypassword;Sslmode=Require

# AWS RDS PostgreSQL
# POSTGRES_CONNECTION=Host=myinstance.123456789012.us-west-2.rds.amazonaws.com;Port=5432;Database=ticketing_platform;Username=postgres;Password=mypassword;Sslmode=Require

# Render PostgreSQL
# DATABASE_URL=postgres://username:password@dpg-xyz123.oregon-postgres.render.com/database_name

# Railway PostgreSQL
# DATABASE_URL=postgres://postgres:password@containers-us-west-xyz.railway.app:1234/railway
